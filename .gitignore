# Build output
/bin/
*.exe
*.dll

# Downloaded ASR models (large; fetched via download-model.ps1)
/models/

# Whisper model cache (downloaded at runtime into recordings/models)
/recordings/

# Runtime
/logs/
config.yaml
hotwords.txt

# Frontend build + deps
/web/build/
/web/node_modules/
/web/.svelte-kit/
