services:
  postgres:
    image: postgres:17
    ports:
      - "5434:5432"
    environment:
      POSTGRES_USER: dndscribe
      POSTGRES_PASSWORD: dndscribe
      POSTGRES_DB: dndscribe
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
